/**
 * bill-analyzer.ts
 *
 * Convenience barrel for consumers that only need the analyzer contract
 * and its associated types (without the factory).
 */
export type { BillAnalyzer, RawBillData, DeterministicBillFlags, SpecialistAnalysis } from './types'
export { AnalyzerError } from './types'
